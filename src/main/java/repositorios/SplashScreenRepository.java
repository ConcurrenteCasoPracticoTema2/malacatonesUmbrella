package repositorios;

import entidades.SplashScreen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SplashScreenRepository extends JpaRepository<SplashScreen, Integer> {
}
